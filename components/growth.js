
export let stateRef, wiz;
export function init(state, W){ stateRef = state; wiz = W; document.getElementById('addGrowth').addEventListener('click', openAddGrowthModal); document.getElementById('btnGenerateGoalModal').addEventListener('click', openGoalModal); renderAll(); }
function openAddGrowthModal(){ const form=document.createElement('form'); form.innerHTML=`<label>Tytuł (cel)<br><input name="title" required></label><br><label>Notatka / link<br><input name="note"></label><br><div style="display:flex;gap:8px;margin-top:8px"><button class="btn" type="submit">Zapisz 💾</button><button type="button" id="cancel" class="btn">Anuluj</button></div>`; form.addEventListener('submit',(e)=>{ e.preventDefault(); const fd=new FormData(form); stateRef.growth.push({ id: Date.now().toString(), title: fd.get('title'), note: fd.get('note'), done:false }); wiz.registerChange(); renderAll(); document.getElementById('modal').classList.add('hidden'); }); form.querySelector('#cancel').addEventListener('click', ()=> document.getElementById('modal').classList.add('hidden')); const openModal = document.querySelector('body').__openModal || window.openModal; if (openModal) openModal('Dodaj cel rozwojowy', form); }
function openGoalModal(){ const form=document.createElement('form'); form.innerHTML=`<label>Wpisz cel (np. Zwiększyć sprzedaż w sklepie o 20%)<br><input name="goal" required></label><br><label>Kategoria (opcjonalnie)<br><input name="category"></label><br><div style="display:flex;gap:8px;margin-top:8px"><button class="btn" type="submit">Generuj plan ✨</button><button type="button" id="cancel" class="btn">Anuluj</button></div>`; form.addEventListener('submit',(e)=>{ e.preventDefault(); const fd=new FormData(form); const goal = fd.get('goal'); const category = fd.get('category'); const steps = generatePlan(goal, category); const div=document.createElement('div'); steps.forEach((s,idx)=>{ const row=document.createElement('div'); row.style.display='flex'; row.style.justifyContent='space-between'; row.style.marginBottom='6px'; row.innerHTML = `<div>${idx+1}. ${escapeHtml(s)}</div><button class="btn">Dodaj jako zadanie</button>`; row.querySelector('button').addEventListener('click', ()=>{ stateRef.tasks.push({ id: Date.now().toString(), title: s, due:null, q:2, priority:2, note:'' }); wiz.registerChange(); }); div.appendChild(row); }); const close=document.createElement('button'); close.className='btn'; close.textContent='Zamknij'; close.addEventListener('click', ()=> document.getElementById('modal').classList.add('hidden')); div.appendChild(close); document.getElementById('modalBody').innerHTML=''; document.getElementById('modalBody').appendChild(div); }); form.querySelector('#cancel').addEventListener('click', ()=> document.getElementById('modal').classList.add('hidden')); const openModal = document.querySelector('body').__openModal || window.openModal; if (openModal) openModal('Zamień cel w plan', form); }
function generatePlan(goal, category){ const steps=[]; steps.push('Zdefiniuj konkretny miernik sukcesu (np. +20% sprzedaży w 3 miesiące).'); steps.push('Przeanalizuj aktualne dane sprzedaży i wybierz top 3 produkty do promocji.'); steps.push('Zbierz feedback klientów (krótka ankieta) i popraw opisy produktów.'); steps.push('Stwórz jedno działanie marketingowe (newsletter lub social) z promocją.'); steps.push('Zaprogramuj test i mierz wyniki przez 2 tygodnie; iteruj.'); return steps; }
function escapeHtml(s){ return String(s).replace(/[&<>"]/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); }
export function renderAll(){ const container=document.getElementById('growthList'); container.innerHTML=''; if(stateRef.growth.length===0){ const hints=[{title:'Kurs UX podstawy — sentobeauty.pl', note:'sprawdź ofertę'},{title:'Szkolenie AI — wprowadzenie', note:'materiały online'}]; hints.forEach(h=>{ const el=document.createElement('div'); el.className='growth-item'; el.innerHTML=`<div>${escapeHtml(h.title)}</div><div><small>${escapeHtml(h.note)}</small></div>`; container.appendChild(el); }); } else { stateRef.growth.slice().reverse().forEach(g=>{ const el=document.createElement('div'); el.className='growth-item'; el.innerHTML=`<div>${escapeHtml(g.title)}</div><div><button class="btn small">${g.done ? '✔' : '☐'}</button></div>`; el.querySelector('button').addEventListener('click', ()=>{ g.done=!g.done; wiz.registerChange(); renderAll(); }); container.appendChild(el); }); } }
